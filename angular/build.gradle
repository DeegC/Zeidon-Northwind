// The build file is only used to build the angular code when building
// the entire Northwind system.  Generally just use npm commands for dev work.
description ="""
Angular application for Northwind.

Project name: ${project.name}
"""


